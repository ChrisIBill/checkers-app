export const CLIENT_PATHS = [
    "/",
    "/Auth",
    "/Auth/Login",
    "/App",
    "/Games",
    "/Games/Checkers",
] as const;

export const NEW_CLIENT_PATHS = [
    "/",
    "/Auth",
    "/Guest",
    "/User",
    "/Admin",
] as const;
